import 'package:flutter/material.dart';

class TowACarPage extends StatelessWidget {
  const TowACarPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Tow a Car'));
  }
}
